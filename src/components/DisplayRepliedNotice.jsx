import { Typography } from "@mui/material";

const DisplayRepliedNotice = () => {
  return (
    <>
      <Typography marginLeft={0.5} variant={"caption"} color={"grey"}>
        Replied
      </Typography>
    </>
  );
};

export default DisplayRepliedNotice;
