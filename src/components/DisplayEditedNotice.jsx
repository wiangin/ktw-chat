import { Typography } from "@mui/material";

const DisplayEditedNotice = () => {
  return (
    <>
      <Typography marginLeft={0.5} variant={"caption"} color={"grey"}>
        Edited
      </Typography>
    </>
  );
};

export default DisplayEditedNotice;
